To build and run, just use the `air` command line command from inside the myapp
directory.

If air is not installed or not in the bin path, you can manually build and run
(from inside the myapp directory) using

`go build -o ./tmp/main ./cmd/web`

`./tmp/main`
